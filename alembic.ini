[alembic]
script_location = %(here)s/alembic
path_separator = os

# Permite que env.py importe el paquete `app`.
prepend_sys_path = .

# La URL se resuelve en env.py a partir de DATABASE_URL. Se deja vacia a proposito:
# ninguna credencial debe vivir en el repositorio.
sqlalchemy.url =

# Las migraciones autogeneradas se formatean y ordenan con ruff, para que cumplan el
# mismo estilo que el resto del codigo sin retoques manuales.
[post_write_hooks]
hooks = ruff_fix,ruff_format
ruff_fix.type = module
ruff_fix.module = ruff
ruff_fix.options = check --fix REVISION_SCRIPT_FILENAME
ruff_format.type = module
ruff_format.module = ruff
ruff_format.options = format REVISION_SCRIPT_FILENAME

[loggers]
keys = root,sqlalchemy,alembic

[handlers]
keys = console

[formatters]
keys = generic

[logger_root]
level = WARNING
handlers = console
qualname =

[logger_sqlalchemy]
level = WARNING
handlers =
qualname = sqlalchemy.engine

[logger_alembic]
level = INFO
handlers =
qualname = alembic

[handler_console]
class = StreamHandler
args = (sys.stderr,)
level = NOTSET
formatter = generic

[formatter_generic]
format = %(levelname)-5.5s [%(name)s] %(message)s
datefmt = %H:%M:%S
